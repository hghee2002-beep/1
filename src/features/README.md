# Features

Feature folders own application use cases and compose domain rules with explicit infrastructure ports. Route handlers and server actions call feature services after authentication and input validation.
